import java.util.*;
import java.util.stream.*;

public class Sample {
  public static void main(String[] args) {
    //A pure function does not have any side-effects.
    //A pure function will give the same output for the same input, no matter how
    //may times we call it.
   
    //lambdas and closures *have* to be pure, even when a language does not protect us.

    //Two rules for purity:
    //1. A pure function should not change any externally visible state
    //2. A pure function should not depend on any external state that may possibly change
  }
}

